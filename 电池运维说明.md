# 电池运维说明（低电量筛选 + 换电路线规划）

> 适用范围：本项目的“电池运维”模块（前端页面 + Flask 后端 + `algorithms/battery_opt.py`）。
>
> 结论先说：当前换电路线计算**在课程/演示场景下是合理的启发式方案**（能体现“容量约束、多车分工、沿路网走、给出服务顺序”）；但它**不是严格最优**（不是标准 VRP/TSP 求解），分组与排序是贪心策略，真实运营场景还需要更多约束与更强的优化器。

---

## 1. 功能目标

电池运维模块解决的问题是：

1. **从全部车辆中筛出“低电量车辆”**（电量 ≤ 阈值）。
2. 给出若干辆“换电运维车”的任务分配：
   - 每辆运维车负责一组低电量车辆（受单次可服务容量 `capacity_per_trip` 约束）。
   - 每组内部给出一个**服务顺序**（第 1 辆、第 2 辆……）。
3. 在地图上绘制每条换电路线（带方向箭头）。
4. 支持 **管理员**（生成/下发任务）与 **调度员**（按负责车辆查看自己的路线与顺序）两类视图。

---

## 2. 端到端流程（前端视角）

### 2.1 管理员流程

1. 登录选择“管理员”。
2. 在“电池运维”模块：
   - 点击“筛选低电量车辆” → 调用 `GET /api/battery/low`。
   - 点击“生成换电路线” → 调用 `POST /api/battery/route`。
3. 前端根据返回结果：
   - 地图：绘制每条路线折线 + 箭头；统一绘制 1 个“校园中心补给点（起终点）”。
   - 表格：展示低电量车辆列表，并显示“所属路线/服务顺序”。

### 2.2 调度员流程

1. 登录选择“调度员”。
2. 调度员界面为只读：
   - 输入“运维车编号/路线编号”（例如：`1`、`battery_service_1`、`换电运维车1`、`换电任务路线1`）
   - 点击查询 → 只显示**自己负责**的那条路线与车辆服务顺序。

> 注意：调度员能看到什么，取决于浏览器中是否已有管理员生成的路线结果（前端会将结果写入 localStorage；但本项目为了避免“遗留操作”，在每次登录后会清空该状态，详见第 7 节）。

---

## 3. 后端接口说明（Flask）

### 3.1 `GET /api/battery/low`

- 作用：返回低电量车辆列表。
- 入参：
  - `threshold`（默认 30）：电量阈值（≤ threshold 算低电）。
  - `min_count`（默认 40）：用于演示的最小返回数量（样本不足会自动补强生成）。
- 出参（简化）：

```json
{
  "threshold": 30,
  "bikes": [
    {"id":"ebike_xxx","lng":114.36,"lat":30.53,"battery":12,"last_used":"08:15"}
  ]
}
```

### 3.2 `POST /api/battery/route`

- 作用：生成多车换电路线（含服务顺序、折线点、统计指标）。
- 入参（前端发送的关键字段）：

```json
{
  "bikes": [{"id":"ebike_1","lng":114.36,"lat":30.53,"battery":12,"last_used":"08:15"}],
  "service_points": [{"lng":114.365,"lat":30.533,"name":"校园中心补给点"}],
  "threshold": 30,
  "capacity_per_trip": 6
}
```

- 出参（简化结构）：

```json
{
  "routes": [
    {
      "vehicle_id": "battery_service_1",
      "vehicle_name": "换电运维车1",
      "route_name": "换电任务路线1",
      "capacity_per_trip": 6,
      "service_point": {"lng":114.365,"lat":30.533},
      "start_point": {"lng":114.365,"lat":30.533},
      "end_point": {"lng":114.365,"lat":30.533},
      "ordered_bikes": [
        {"id":"ebike_7","lng":114.361,"lat":30.532,"battery":11,"service_order":1}
      ],
      "points": [{"lng":114.365,"lat":30.533},{"lng":114.3649,"lat":30.5329}],
      "total_distance_m": 1234.5
    }
  ],
  "vehicle_count": 7,
  "route_count": 7,
  "bike_count": 40,
  "capacity_per_trip": 6,
  "total_distance_m": 8888.8,
  "bike_assignments": {
    "ebike_7": {"vehicle_id":"battery_service_1","route_name":"换电任务路线1","service_order":1}
  }
}
```

---

## 4. 低电量车辆数据是怎么来的（`get_low_battery_vehicles`）

后端函数：`algorithms/battery_opt.py:get_low_battery_vehicles(threshold, min_count, data_dir, roads_geojson)`

1. **优先读取本地文件**：`data_bd09/battery_mock.json`（若存在）。
2. 解析出车辆记录并归一化字段：
   - `id`：统一为字符串，且保证以 `ebike_` 前缀开头。
   - `lng/lat`：必须可转为 float。
   - `battery`：缺省时按 100 处理。
   - `last_used`：兼容 `lastUsed/time_slot` 等字段。
3. 筛选低电：`battery <= threshold`。
4. 如果低电车辆数量 `< min_count`：为了演示效果，会**自动生成补充数据**：
   - 优先从路网 GeoJSON 里抽取路网点作为候选位置，再加小范围随机抖动。
   - 电量随机生成在 `[5, max(12, threshold)]`。
   - 随机种子固定（与日期/数量相关），保证同条件下结果相对稳定。
5. 返回按电量升序排序的低电车辆列表。

> 这一步的“补强生成”是演示友好设计：保证地图上始终有足够多的低电车辆可用于分车、规划路线、展示表格排序等效果。

---

## 5. 换电路线算法怎么算的（`plan_battery_routes`）

后端函数：`algorithms/battery_opt.py:plan_battery_routes(low_bikes, service_points, roads_geojson, capacity_per_trip)`

### 5.1 输入与关键参数

- `low_bikes`：低电车辆列表（含 `lng/lat/battery/id`）。
- `capacity_per_trip`：每辆运维车一次最多服务多少辆车（默认 6）。
- `service_points`：补给点列表（前端按最新规则固定传 1 个：校园中心补给点）。
- `roads_geojson`：路网（`data_bd09/WHUInfo_Roads.geojson`）。

### 5.2 需要几辆运维车（容量约束）

令低电车辆数为 $N$，每车容量为 $C$：

$$\text{vehicle\_count} = \lceil N / C \rceil$$

这保证“平均意义上”每条路线服务车辆数不超过容量（本实现采用“先分组再每组一车”，实际每组大小由分组结果决定）。

### 5.3 低电车辆如何分组（每辆车负责哪些点）

使用 `_spatial_chunks(low_bikes, vehicle_count)`：

1. 先按 `(lng, lat, id)` 排序。
2. 将排序后的列表按块切分为 `vehicle_count` 份（块大小约为 `ceil(N/vehicle_count)`）。

这种分组方式的特点：
- 实现简单、可解释、稳定。
- 但它**不是严格的空间聚类**（不是 k-means/DBSCAN），当车辆分布很不均匀时，可能导致某些路线跨区域。

### 5.4 每组内的服务顺序怎么排（最近邻贪心）

对每一组 `group`：

- 起点为补给点 `service_point`（前端强制统一为校园中心）。
- 用 `_nearest_neighbor_order(service_point, group)`：
  - 当前点从补给点开始。
  - 每一步选择“离当前点最近的下一辆车”。
  - 直到访问完组内所有车辆。

这属于经典的**最近邻启发式**，能快速得到一条“看起来合理”的路径，但不保证最短。

### 5.5 路线折线怎么生成（沿路网最短路拼接）

为了让路线尽量“沿着路走”，后端会将“补给点 → 第 1 辆车 → 第 2 辆车 → … → 返回补给点”拆成多段，并逐段用路网最短路连接：

1. `build_road_graph(roads_geojson)`：
   - 把路网 GeoJSON 的 LineString/MultiLineString 顶点作为图节点。
   - 相邻顶点连边，边权用 `haversine` 距离（米）。
2. 对于任意两点 A、B：
   - 找到各自最近的路网节点（`_nearest_node_id`，暴力扫描所有节点）。
   - 在图上用 Dijkstra（`_shortest_path_node_ids`）求最短路径。
   - 将路径节点坐标还原成点列，并在头尾补回 A、B 的原始坐标（`_connect_points_via_road`）。
3. 把每段点列拼成整条 `polyline`，并做连续重复点去重。
4. 计算路线长度：对 polyline 相邻点做 haversine 累加（`_polyline_distance`）。

> 兜底：如果路网为空或某段最短路失败，会退化为“直线连接 A→B”。

### 5.6 输出结构（路线 + 分配表）

- `routes[]`：每条路线包含：
  - `vehicle_id/vehicle_name/route_name`
  - `ordered_bikes[]`（含 `service_order`）
  - `points[]`（用于前端画折线）
  - `start_point/end_point`（同一个补给点）
  - `total_distance_m`
- `bike_assignments`：`bike_id -> {vehicle_id, vehicle_name, route_name, service_order}` 方便前端表格直接展示。

---

## 6. 前端是怎么实现与渲染的（`static/script.js`）

### 6.1 管理员操作按钮

- `filterLowBattery()`：
  - 调 `GET /api/battery/low` 取低电列表。
  - 更新表格、低电数量指标、地图 marker。
- `generateBatteryRoute()`：
  - 组装 `service_points`（固定校园中心）+ `bikes` + `capacity_per_trip`。
  - 调 `POST /api/battery/route`。
  - 调 `drawBatteryRoute()` 绘制。

### 6.2 路线绘制与箭头方向

- `drawBatteryRoute(routeResult)`：
  - 为每条 route：
    - `BMap.Polyline` 画折线。
    - `addBatteryRouteArrows(points, color)` 添加若干箭头 marker。
  - 统一绘制 1 个“换电路线起终点”标注在校园中心（避免每条路线重复标注起终点）。

- `addBatteryRouteArrows()`：
  - 用相邻两点计算方向角：`atan2(dy, dx)`。
  - 由于百度 Symbol 箭头的朝向基准与数学角度存在偏置，使用 `rotation = angle + 90` 修正（`BATTERY_ARROW_ROTATION_OFFSET = 90`）。

### 6.3 表格排序与“所属路线/服务顺序”

- `renderBatteryTableRows()` 会优先按 `batteryRouteAssignments[bike.id].service_order` 升序排序；没有 service_order 的再按电量排序。

### 6.4 调度员仅看自己负责路线

- `applyDispatcherVehicleFilter()`：
  - 从 `batteryLastRouteResult.routes` 里匹配 route（支持输入 `1`/`换电运维车1`/`battery_service_1` 等）。
  - 只渲染该 route 的 bikes 与折线，并把表格范围缩小到该 route。

---

## 7. 状态持久化与“遗留操作”为何会出现、如何处理

### 7.1 为什么会出现“打开电池运维页有上次遗留操作”

前端会把电池运维的关键状态持久化到浏览器 localStorage：

- Key：`battery_ops_state_v1`
- 内容包括：阈值、容量、低电列表、路线结果、分配表、调度员上次查询的车辆 key。

因此，如果不清理，这些内容会在下次打开页面时被恢复（`hydrateBatteryOpsView()`）。

### 7.2 当前项目的处理策略

为满足“不要遗留操作”的要求：

- 登录成功后会执行 `resetBatteryOpsStateForNewLogin()`：
  - 删除 localStorage 的 `battery_ops_state_v1`
  - 清空内存中的路线/marker/table 状态
  - 重置 UI 输入与统计面板

这意味着：
- **跨登录会话**不再保留上一次的电池运维结果。
- 在同一次登录期间，仍会在你点击筛选/生成/查询后写入快照（便于页面切换时不丢）。

---

## 8. 当前路线计算“合理吗？”——评估与局限

### 8.1 合理性（为什么说在演示场景下合理）

当前算法具备“运维任务规划”最关键的可解释要素：

- **容量约束**：容量 C 决定需要几辆车（$\lceil N/C \rceil$）。
- **多车分工**：每辆车负责一组低电车辆。
- **服务顺序**：每组给出明确的 `service_order`。
- **沿路网行驶**：路线折线由路网图上的最短路拼接而成，而不是简单直线。
- **结果可视化友好**：每条路线有颜色、箭头，表格能看到路线与顺序。

对课程展示而言，这些要点往往比“绝对最短”更重要。

### 8.2 局限（它为什么不一定最优）

1. **分组方法是排序切块**：可能造成某些路线跨区域、或出现路线交叉。
2. **顺序方法是最近邻贪心**：不保证全局最短；容易受起点选择影响。
3. **路网最短路的成本函数很简化**：
   - 边权只用直线距离（haversine），不考虑路宽、坡度、禁行、转向成本、红绿灯等。
4. **性能上存在潜在瓶颈**：
   - 每段连接都要“遍历全图找最近节点” + 跑一次 Dijkstra；当路网节点很多、低电车很多时会变慢。

### 8.3 如果要更贴近真实运维，可以怎么改（建议清单）

- 分组：用 k-means（按经纬度）或基于路网距离的聚类，减少跨区。
- 排序：对每组做 2-opt/3-opt 局部搜索，或用 OR-Tools 求解小规模 VRP。
- 目标函数：把“距离 + 预计用时 + 电量风险 + 车辆优先级”融合成综合成本。
- 约束：加入“必须在某时间窗前服务”“电池更换耗时”“运维车续航/载荷”等。

---

## 9. 常见问题（FAQ）

### Q1：为什么低电车辆数量看起来很多、而且分布很均匀？
A：`GET /api/battery/low` 默认 `min_count=40`，若真实样本不足会自动补强生成，用于演示分车与路线规划效果。

### Q2：为什么所有路线都从同一个点出发/回到同一个点？
A：前端 `generateBatteryRoute()` 固定传入服务点为“校园中心补给点”，后端按该点作为 `start_point/end_point`。

### Q3：调度员看不到路线？
A：调度员只读且依赖“已生成的任务结果”。此外，本项目为了避免遗留操作，会在每次登录后清空本地持久化状态；所以需要管理员在本次登录中生成一次路线，调度员才有可查询的内容。

---

## 10. 代码索引（便于快速定位）

- 后端：
  - `app.py`：`/api/battery/low`、`/api/battery/route`
  - `algorithms/battery_opt.py`：`get_low_battery_vehicles()`、`plan_battery_routes()`
- 前端：
  - `static/index.html`：电池运维模块 UI（管理员/调度员控件区、表格、图例）
  - `static/script.js`：`filterLowBattery()`、`generateBatteryRoute()`、`drawBatteryRoute()`、`applyDispatcherVehicleFilter()`、`hydrateBatteryOpsView()`
