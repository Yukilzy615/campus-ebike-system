# 校园电单车智能选址与动态调度管理系统

## 项目设计说明书

**参赛赛道**：管理信息系统\
**参赛组别**：本科组\
**作品名称**：校园电单车智能选址与动态调度管理系统

***

## 摘要

随着共享电单车在高校校园内的普及，停车点规划不合理、供需失衡、调度效率低下等问题日益突出。传统的人工管理方式依赖经验和微信群沟通，缺乏数据支撑和信息化管理工具，难以应对复杂的校园环境和动态变化的需求。本系统以武汉大学信息学部为示范场景，基于真实校园地理空间数据，设计并实现了一套集**选址规划管理**、**调度任务管理**、**车辆运维管理**、**可视化分析**与**智能辅助决策**于一体的电单车运营管理信息系统。系统采用前后端分离架构，前端基于Leaflet与百度地图API实现多图层地图可视化，后端集成遗传算法（NSGA-II）与蚁群算法（ACO）提供决策支持，并设计了管理员与调度员双角色任务闭环，实现了从方案制定、任务下发、执行反馈到数据监控的完整业务流程管理。在此基础上，系统引入智能辅助模块，对优化结果进行解释、生成调度待办清单、提供风险预警，有效提升了管理决策的科学性和系统的人机协作水平。

**关键词**：管理信息系统；校园电单车；选址规划；调度管理；可视化分析；智能决策支持

***

## 一、项目背景与意义

### 1.1 管理现状与痛点

近年来，共享电单车已成为高校师生校内短途出行的重要交通工具。然而，在校园电单车运营管理中普遍存在以下问题：

- **停车点规划缺乏科学依据**：现有停车点多依靠经验划定，部分区域车辆堆积，部分区域一车难求，供需空间错配严重，缺乏量化的选址规划管理工具。
- **调度管理手段原始**：调度指令依赖微信群通知，任务派发、执行反馈、状态跟踪均无系统记录，调度效率低下且难以追溯。
- **运维管理碎片化**：低电量车辆需人工巡检发现，换电任务安排缺乏优先级规划，电池状态无法实时监控。
- **管理决策缺乏数据支撑**：管理者无法直观掌握车辆分布、需求热点、调度效果等关键指标，难以对运营方案进行科学评估与优化。

### 1.2 管理信息系统建设的必要性

针对上述问题，构建一个专门的校园电单车运营管理信息系统具有以下必要性：

1. **实现业务流程标准化**：将选址规划、调度任务派发、执行反馈、运维监控等业务环节纳入统一系统管理，规范操作流程。
2. **提升决策科学化水平**：通过数据分析和智能算法，为选址规划、调度方案制定提供量化依据，减少经验决策的盲目性。
3. **加强任务执行可追溯性**：记录调度任务的生成、派发、执行、完成全流程状态，便于绩效考核和问题追溯。
4. **提高人机协作效率**：引入智能辅助模块，将复杂的算法结果转化为可理解的管理建议，降低系统使用门槛。

### 1.3 系统目标

本作品针对上述管理需求，设计并实现一个完整的校园电单车运营管理信息系统，具体目标包括：

1. **选址规划管理**：支持基于多目标优化算法的智能选址，以及人工手动调整选址方案，实现选址方案的数字化管理。
2. **调度任务管理**：根据时段需求自动生成调度方案，支持任务派发、状态跟踪和执行反馈，形成完整的任务闭环。
3. **车辆运维管理**：实时监控车辆电量状态，筛选低电量车辆，生成换电任务路线。
4. **可视化监控分析**：通过地图、图表等多种形式，直观展示运营核心指标，辅助管理决策。
5. **智能辅助决策**：引入智能体模块，对优化结果进行解释、生成任务优先级清单、提供风险预警，提升系统的管理决策支持能力。

***

## 二、系统总体设计

### 2.1 系统架构

系统采用**前后端分离**的B/S架构，整体分为数据层、业务逻辑层、服务层、智能辅助层和展示层五个层次。

```
┌─────────────────────────────────────────────────────────────────┐
│                      展示层（前端）                              │
│  HTML5 / CSS3 / JavaScript / Leaflet / Chart.js                 │
│   地图可视化 │ 图表看板 │ 角色工作台 │ 任务面板 │ 参数配置       │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                   智能辅助层（AI Agent）                         │
│  方案解释 │ 调度待办生成 │ 风险预警 │ 决策问答                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      服务层（Flask）                             │
│   RESTful API  │  请求路由  │  数据聚合  │  静态资源托管         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    业务逻辑层（Python）                          │
│  选址规划引擎（NSGA-II）│ 调度规划引擎（ACO）│ 模拟数据生成器     │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      数据层                                      │
│  GeoJSON（路网/建筑/POI/边界）│ CSV（需求数据）│ 任务状态数据     │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 类别      | 技术/框架                     | 用途              |
| :------ | :------------------------ | :-------------- |
| **前端**  | HTML5 / CSS3 / JavaScript | 页面结构与样式         |
| **前端**  | Leaflet                   | 地图图层管理与交互       |
| **前端**  | 百度地图API（v3.0）             | 合规底图展示          |
| **前端**  | Chart.js                  | 数据可视化图表         |
| **前端**  | gcoord                    | WGS84↔BD09坐标转换  |
| **后端**  | Flask                     | Web服务器与API接口    |
| **后端**  | NumPy / Pandas            | 科学计算与数据处理       |
| **后端**  | GeoPandas / Shapely       | 地理空间数据处理        |
| **后端**  | NetworkX                  | 路网构建与最短路径分析     |
| **后端**  | DEAP                      | 遗传算法（NSGA-II）实现 |
| **后端**  | 自研蚁群算法（ACO）               | 调度路径优化          |
| **智能体** | 大赛指定AI工具                  | 结果解释、任务生成、风险预警  |

### 2.3 数据来源

系统以**武汉大学信息学部**为示范区域，使用以下真实地理数据：

| 数据名称     | 文件名                        | 格式      | 说明                 |
| :------- | :------------------------- | :------ | :----------------- |
| 道路网络     | WHUInfo\_Roads.geojson     | GeoJSON | 校园机动车道、人行道、内部道路    |
| 建筑物轮廓    | WHUInfo\_Buildings.geojson | GeoJSON | 宿舍、教学楼、食堂、实验室等建筑面  |
| 兴趣点（POI） | WHUInfo\_Points.geojson    | GeoJSON | 校门、食堂、运动场等重要点位     |
| 校园边界     | WHUInfo\_Area.geojson      | GeoJSON | 信息学部精确边界多边形        |
| 需求数据     | demands.csv                | CSV     | 不同时段各区域电单车需求强度（模拟） |

所有空间数据均为**WGS84坐标系**，前端展示时通过`gcoord`库统一转换为百度**BD09坐标系**，确保与百度地图底图精准叠加。百度地图底图自动展示合规审图号，符合国家测绘地理信息法规要求。

### 2.4 功能模块划分

系统围绕电单车运营管理的核心业务流程，划分为六大功能模块：

| 模块名称       | 主要功能                   | 对应管理业务 |
| :--------- | :--------------------- | :----- |
| **系统概览**   | 全局指标监控、图层控制、方案导出       | 管理驾驶舱  |
| **选址规划管理** | 智能选址、人工选址、方案对比、方案审批    | 基础设施规划 |
| **调度任务管理** | 调度方案生成、任务派发、状态跟踪、路线可视化 | 日常运营调度 |
| **电池运维管理** | 低电量车辆筛选、换电任务生成、换电路线规划  | 资产运维   |
| **车辆模拟监控** | 车辆分布模拟、移动轨迹回放          | 运营监控   |
| **智能辅助决策** | 方案解释、任务优先级建议、风险预警、决策问答 | 决策支持   |

### 2.5 角色与权限设计

系统设计了两种角色，对应电单车运营管理中的实际岗位分工：

| 角色      | 职责描述                 | 系统权限                    |
| :------ | :------------------- | :---------------------- |
| **管理员** | 负责选址规划、调度方案制定、全局运营监控 | 全部功能模块访问权限，可进行方案制定与参数配置 |
| **调度员** | 负责接收调度任务、执行任务并反馈状态   | 仅访问任务看板、调度路线查看、任务状态更新功能 |

系统在登录时根据角色选择动态加载对应的菜单和默认工作台，实现了基于角色的访问控制（RBAC）。

***

## 三、核心业务流程设计

### 3.1 选址规划管理流程

选址规划是电单车运营管理的基础性工作，本系统设计了“智能生成—人工调整—对比评估—方案确认”的规范化流程：

1. **智能选址生成**：管理员设置停车点数量、服务半径、优化目标（覆盖率/步行距离/负载均衡），系统调用NSGA-II算法生成推荐选址方案。
2. **人工调整**：管理员可在地图上手动添加、拖拽、删除停车点，形成人工方案或对智能方案进行微调。
3. **方案对比评估**：系统通过雷达图对比两种方案在覆盖率、平均步行距离、均衡性、总容量四个维度上的表现。
4. **智能辅助解释**：智能体自动生成对比简报，指出方案优劣和推荐理由，辅助管理员决策。
5. **方案确认**：管理员确认最终方案，系统将其设为“当前使用方案”，供后续调度模块调用。

### 3.2 调度任务管理流程

调度任务管理是日常运营的核心环节，系统实现了“方案生成—任务拆解—派发执行—状态反馈”的完整闭环：

1. **调度方案生成**：管理员选择调度时段，系统基于当前选址方案和时段需求规则，调用ACO算法生成最优调度路线。
2. **任务拆解**：调度路线被自动拆解为若干具体任务项（如“从A点运X辆至B点”），每项任务包含起点、终点、转运量、预计距离和耗时。
3. **任务派发**：任务自动推送至调度员工作台的“待执行任务”列表。
4. **执行与反馈**：调度员查看任务详情和推荐路线，执行任务后点击“标记完成”，任务状态流转至“已完成”。
5. **智能辅助建议**：智能体基于当前调度结果生成优先级任务清单，提示调度员优先处理的关键任务。

### 3.3 电池运维管理流程

电池运维是保障车辆可用性的重要工作，系统提供以下管理功能：

1. **低电量监控**：系统实时筛选电量低于设定阈值的车辆，列表展示车辆ID、位置、电量。
2. **换电任务生成**：一键生成换电路线，系统将低电量车辆作为特殊“需求点”，复用调度算法生成最优换电路线。
3. **任务执行跟踪**：换电任务同样纳入调度员任务看板统一管理。

### 3.4 数据监控与分析流程

系统提供多维度数据监控与分析功能，支撑管理决策：

- **核心指标看板**：实时展示覆盖率、平均步行距离、均衡性指数、总容量等关键指标。
- **供需状态监控**：分时段展示各停车点的当前车辆数、需求数、状态（过剩/不足/平衡）及建议转运量。
- **历史趋势分析**：通过时段使用分布图展示各时段的实际使用量与预测需求，辅助管理者把握需求规律。

***

## 四、决策支持算法设计

### 4.1 智能选址算法——基于NSGA-II的多目标优化

#### 4.1.1 问题建模

智能选址问题可形式化为一个**多目标设施选址问题**：在校园路网的候选节点集合中，选择K个点作为电单车停车点，使得以下三个目标同时达到最优：

- **最大化服务覆盖率**：服务半径内覆盖的需求点数量占比。
- **最小化平均步行距离**：所有需求点到最近停车点的平均路网距离。
- **最小化负载不均衡度**：各停车点服务需求量的变异系数。

#### 4.1.2 算法实现

采用DEAP框架实现带精英策略的非支配排序遗传算法（NSGA-II）。

- **编码方式**：定长二进制编码，值为1的基因表示选中，总数恒为K。
- **适应度函数**：`f1 = -覆盖率`，`f2 = 平均路网距离 + 过密惩罚`，`f3 = 负载均衡系数`。
- **遗传算子**：锦标赛选择、两点交叉（带修复）、位翻转变异（带修复）。
- **参数设置**：种群大小20，迭代代数50，交叉概率0.7，变异概率0.2。

#### 4.1.3 输出与应用

算法返回帕累托前沿解集，根据用户选择的优化目标组合筛选最优解，输出为GeoJSON格式的停车点集合，供前端渲染和后续调度使用。

### 4.2 动态调度算法——基于蚁群算法的路径优化

#### 4.2.1 问题描述

给定当前时段各停车点的供需状态（过剩量/短缺量），求一组调度车辆的行驶路线，使得总调度距离最短，同时满足车辆容量约束。

#### 4.2.2 供需状态计算

系统基于功能区中心坐标和时段需求规则，计算每个停车点的净流量（正为供应，负为需求），仅将非零净流量的点纳入调度计算。

#### 4.2.3 蚁群算法设计

自研蚁群算法求解调度路径优化问题。

- **状态表示**：每个停车点为一个节点，包含坐标、供需类型、待转运量。
- **路径构建**：蚂蚁从供应点出发，交替访问供应点和需求点。
- **转移概率**：`P_{ij} = (τ_{ij})^α * (η_{ij})^β / Σ(τ_{ik})^α * (η_{ik})^β`，α=1，β=3。
- **信息素更新**：全局更新策略，仅最优路径增强。

#### 4.2.4 输出与应用

算法生成点对点调度路线，输出为GeoJSON LineString，并自动拆解为可执行的任务项，推送至调度员工作台。

### 4.3 智能辅助决策模块

#### 4.3.1 模块定位

智能体模块的核心定位是：**“看懂结果，并告诉人该怎么用”**。它不替代选址与调度核心算法，而是作为算法的“解释器”和管理者的“参谋”，负责对算法输出结果进行自然语言解释、生成可执行的任务清单、提供风险预警，从而提升管理信息系统的决策支持能力和人机协作水平。

#### 4.3.2 功能场景

| 功能          | 接入位置     | 作用描述                             |
| :---------- | :------- | :------------------------------- |
| **方案解释**    | 选址方案对比页面 | 自动总结两种方案的指标差异，解释推荐理由，指出覆盖优劣区域    |
| **调度任务优先级** | 调度结果页面   | 基于当前调度结果生成优先级任务清单，提示优先处理的补车点/需求点 |
| **风险预警**    | 系统概览页    | 识别长期缺车区域、低利用率停车点、覆盖率过低等风险并给出建议   |
| **决策问答**    | 智能辅助面板   | 用户选择优化偏好，智能体输出对应的推荐方案及理由         |

#### 4.3.3 与核心算法的关系

智能体**不参与**优化计算过程，仅在优化完成后接收结果数据和上下文信息，输出人类可读的解释和建议。这种设计确保了核心算法的专业性和确定性，同时借助大语言模型的语义能力弥补了传统算法在可解释性和人机交互方面的不足。

***

## 五、系统实现与关键技术

### 5.1 地图可视化实现

系统底图采用**百度地图API**，业务图层基于**Leaflet**渲染，通过`gcoord`库实现WGS84到BD09的坐标转换。主要图层包括：

- **POI图层**：按类别着色，辅助理解需求分布。
- **选址点图层**：智能选址（蓝色三角形）、人工选址（红色三角形）。
- **覆盖范围图层**：半透明圆形缓冲区。
- **调度路径图层**：紫色虚线带箭头，起终点分别用绿/红色圆点标记。
- **校园边界图层**：蓝色虚线多边形。

### 5.2 任务管理实现

调度员工作台设计了看板式任务管理界面，分为“待执行”、“进行中”、“已完成”三列。任务数据存储于前端内存（演示版本），点击“标记完成”按钮触发状态流转。实际应用中可扩展为数据库持久化存储。

### 5.3 数据可视化实现

采用**Chart.js**库实现核心指标的图表展示，包括：

- **雷达图**：方案对比分析。
- **柱状图**：时段使用分布。
- **表格**：供需状态、调度路段详情。

### 5.4 角色权限控制

通过登录页的角色下拉框实现前端层面的菜单显隐控制，无需后端权限验证，满足演示场景下的角色切换需求。

### 5.5 坐标系转换与地图合规性

- 所有自采OSM数据为WGS84坐标系，前端统一通过`gcoord`库转换为BD09坐标系。
- 百度地图底图自动展示完整的版权与审图号信息，符合国家测绘法规要求。

***

## 六、系统测试

### 6.1 测试环境

- **操作系统**：Windows 11 / macOS Ventura
- **浏览器**：Chrome 120+、Edge 120+
- **Python版本**：3.9
- **依赖库**：Flask、GeoPandas、NetworkX、DEAP、NumPy等

### 6.2 功能测试

| 测试项          | 预期结果                 | 实际结果 |
| :----------- | :------------------- | :--- |
| 管理员登录后访问全部功能 | 菜单完整，默认显示系统概览        | ✅通过  |
| 调度员登录后菜单受限   | 隐藏选址分析等高级菜单，默认显示任务看板 | ✅通过  |
| 智能选址生成       | 返回K个停车点，覆盖率指标更新      | ✅通过  |
| 人工选址编辑       | 支持点击添加、拖拽、删除，覆盖率实时更新 | ✅通过  |
| 方案对比与AI解释    | 生成雷达图，智能体输出对比简报      | ✅通过  |
| 调度方案生成       | 生成紫色调度路线，任务自动拆解至看板   | ✅通过  |
| 任务状态流转       | 点击“标记完成”后任务移至已完成列    | ✅通过  |
| 电池运维筛选       | 正确筛选低电量车辆，生成换电路线     | ✅通过  |
| 地图审图号显示      | 百度地图左下角完整显示版权与审图信息   | ✅通过  |

### 6.3 性能测试

- 智能选址（K=10，候选点120）：平均耗时约**8秒**。
- 动态调度（供需点约12个）：平均耗时约**2秒**。
- 智能体API调用：平均耗时约**1.5秒**。
- 前端渲染100辆模拟电单车：无明显卡顿，动画帧率稳定在30fps以上。

***

## 七、系统特色与创新点

1. **完整的管理业务流程闭环**：覆盖选址规划、调度任务派发、执行反馈、运维监控的全链条管理，符合管理信息系统的核心特征。
2. **角色化工作台设计**：区分管理员与调度员视角，贴合实际岗位分工，降低了不同角色的使用门槛。
3. **智能算法与管理系统深度融合**：将NSGA-II和ACO算法作为决策支持引擎嵌入管理流程，实现从“经验决策”到“数据驱动决策”的升级。
4. **智能辅助决策增强可解释性**：创新性地引入智能体模块，将复杂的算法结果转化为易于理解的管理建议，解决了“算法黑箱”问题。
5. **真实数据驱动**：基于武汉大学信息学部真实地理数据构建，算法结果具备实际参考价值。

***

## 八、总结与展望

### 8.1 作品总结

本作品设计并实现了一个完整的校园电单车运营管理信息系统，围绕选址规划、调度任务管理、电池运维管理三大核心业务，构建了从方案制定、任务派发、执行反馈到数据监控的管理闭环。系统集成了NSGA-II多目标优化算法和ACO路径优化算法作为决策支持引擎，并引入智能辅助模块提升系统的可解释性和人机协作水平。系统界面友好、交互流畅，具有良好的实用性和扩展性。

### 8.2 未来展望

- **数据持久化**：当前任务状态存储于前端内存，后续可接入数据库实现持久化存储和历史数据查询。
- **移动端适配**：开发移动端轻量版供调度员现场使用，支持扫码确认任务完成。
- **多校区扩展**：系统已预留校区切换入口，可快速接入其他校区地理数据。
- **实时数据对接**：对接校园电单车运营平台的真实订单和车辆状态数据，实现真正的实时监控与动态调度。

***

## 参考文献

\[1] 中国大学生计算机设计大赛组委会. 2026年（第19届）中国大学生计算机设计大赛竞赛规程\[Z]. 2026.\
\[2] Deb K, Pratap A, Agarwal S, et al. A fast and elitist multiobjective genetic algorithm: NSGA-II\[J]. IEEE Transactions on Evolutionary Computation, 2002, 6(2): 182-197.\
\[3] Dorigo M, Stützle T. Ant Colony Optimization\[M]. MIT Press, 2004.\
\[4] 百度地图开放平台. JavaScript API v3.0 开发文档\[EB/OL]. <https://lbsyun.baidu.com/>.\
\[5] Leaflet. Leaflet 1.9.4 Documentation\[EB/OL]. <https://leafletjs.com/>.\
\[6] 薛华成. 管理信息系统（第7版）\[M]. 清华大学出版社, 2022.

***

**附录**\
A. 系统源代码目录结构\
B. API接口文档\
C. 部署说明\
D. 答辩PPT要点

（附录部分略）
