# 安装及使用

本文档适用于项目根目录：`campus_ebike`。

## 1. 运行环境

- 操作系统：Windows（已在本机验证）
- Python：推荐 `3.12.x`
- 建议使用虚拟环境 `.venv`

## 2. 首次安装

在项目根目录打开 PowerShell，执行：

```powershell
# 1) 创建虚拟环境（推荐使用本机可用的 Python 3.12）
D:\Python3.12.4\python.exe -m venv .venv

# 2) 激活虚拟环境
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
.\.venv\Scripts\Activate.ps1

# 3) 安装依赖
# 若你本机 pip 源默认是清华 HTTP 镜像，请使用 trusted-host 参数
python -m pip install --index-url http://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn -r requirements.txt
```

如果你本机可以直接访问官方源，也可使用：

```powershell
python -m pip install -r requirements.txt
```

## 3. 启动项目

```powershell
python app.py
```

启动成功后会看到类似输出：

- `* Running on http://127.0.0.1:5001`
- `GeoDataFrame加载成功`

浏览器打开：

- http://127.0.0.1:5001

停止服务：按 `Ctrl + C`。

## 4. 我这边已验证通过的结果

已在当前工作区实际跑通：

- 服务启动成功（Flask debug 模式）
- 根路径 `/` 可访问，HTTP 状态码 `200`
- 监听端口：`5001`

## 5. 主要接口（用于联调）

- `GET /api/map-data`：基础地图数据（路网、建筑、POI、区域）
- `GET /api/heatmap-data?time=morning|noon|evening`：热力图点
- `GET/POST /api/optimize-location`：选址优化
- `GET/POST /api/optimize-dispatch`：调度优化
- `GET /api/bike-simulation?time=morning|noon|evening&level=low|medium|high`：电单车模拟
- `POST /api/save-locations`：保存前端编辑停车点
- `GET /api/compare-locations`：算法点与手动点对比
- `GET /api/roads`：过滤后路网

## 6. 常见问题

### 6.1 报错：`No Python at ... WindowsApps ...`

说明当前 `.venv` 已损坏（通常是旧的 Microsoft Store Python 路径失效）。

处理方式：删除并重建虚拟环境。

```powershell
Remove-Item -Recurse -Force .venv
D:\Python3.12.4\python.exe -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --index-url http://pypi.tuna.tsinghua.edu.cn/simple --trusted-host pypi.tuna.tsinghua.edu.cn -r requirements.txt
```

### 6.2 依赖安装提示不信任清华源

当提示 `not a trusted or secure host` 时，在安装命令中加入：

- `--index-url http://pypi.tuna.tsinghua.edu.cn/simple`
- `--trusted-host pypi.tuna.tsinghua.edu.cn`

## 7. 目录说明（简要）

- `app.py`：后端入口（Flask）
- `algorithms/`：选址、调度、模拟算法
- `data/`：需求数据与部分原始数据
- `data_bd09/`：BD09 坐标系地理数据
- `static/`：前端页面与脚本
