# 安装及使用说明

本文档用于快速安装、启动和使用“校园电动车智能调度系统”。该项目已在本地验证可通过 `python app.py` 正常启动，后端默认监听 `5001` 端口。

## 1. 环境要求

- Windows / macOS / Linux
- Python 3.8 或更高版本
- `pip` 包管理器
- 建议使用独立虚拟环境（`venv`、`conda` 均可）

## 2. 项目安装

进入项目根目录后执行以下命令：

```bash
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt --trusted-host pypi.tuna.tsinghua.edu.cn --index-url http://pypi.tuna.tsinghua.edu.cn/simple
```

如果你已经处在 conda 的 `base` 环境里，建议不要直接依赖 `python` 和 `pip` 命令，而是始终使用 `.venv\Scripts\python.exe`。

如果镜像源仍然不可用，也可以改用官方源：

```bash
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

## 3. 启动项目

在项目根目录执行：

```bash
.\.venv\Scripts\python.exe app.py
```

启动成功后，终端会看到 Flask 服务地址，默认是：

- `http://127.0.0.1:5001`
- `http://localhost:5001`

直接在浏览器打开根地址即可进入前端页面。

## 4. 使用流程

1. 启动后端服务。
2. 打开浏览器访问 `http://localhost:5001`。
3. 在页面中使用地图、热力图、智能选址、调度、电池运维和反馈等功能。
4. 如需查看接口返回，可直接访问后端 API。

## 5. 主要功能说明

- 地图数据：加载校园路网、建筑、POI 和区域边界。
- 热力图：支持早高峰、午高峰、晚高峰数据展示。
- 智能选址：调用遗传算法生成推荐停车点。
- 动态调度：生成调度路线，支持管理员和调度员视角。
- 电动车模拟：生成车辆分布与状态模拟数据。
- 电池运维：识别低电量车辆并规划换电路线。
- 意见反馈：提交、查看、更新反馈记录。
- AI 辅助：生成方案解释、待办、风险预警、决策建议和受限问答结果。

## 6. 常用接口

- `GET /api/map-data`：基础地图数据
- `GET /api/heatmap-data?time=morning|noon|evening`：热力图数据
- `GET /api/optimize-location`：智能选址结果
- `GET/POST /api/optimize-dispatch`：调度路线
- `GET /api/bike-simulation`：电动车模拟数据
- `GET /api/battery/low`：低电量车辆列表
- `POST /api/battery/route`：换电路线规划
- `GET /api/feedback/list`：反馈列表
- `POST /api/feedback/create`：提交反馈
- `POST /api/feedback/update`：更新反馈
- `POST /api/ai/chat`：受限问答

## 7. 数据说明

- 地理数据放在 `data/` 目录下。
- 项目启动时会自动读取 `WHUInfo*.geojson` 等文件。
- 如果 `data/demands.csv` 不存在，后端会自动创建默认需求数据。
- 保存手动选址时，会在 `data/manual_locations.geojson` 中生成结果文件。

## 8. 常见问题

### 8.1 依赖安装失败

优先确认 Python 版本和虚拟环境是否正确。
如果是 Windows 且国内网络环境导致 `pip` 访问失败，可更换镜像源后重试安装。

### 8.2 启动后没有页面

请确认后端终端没有报错，并访问 `http://localhost:5001`。
如果端口被占用，可以先关闭占用 `5001` 端口的程序，再重新启动。

### 8.3 地图或空间功能异常

请确认 `data/` 目录中的 GeoJSON 文件完整存在，尤其是：

- `WHUInfo.geojson`
- `WHUInfo_Roads.geojson`
- `WHUInfo_Buildings.geojson`
- `WHUInfo_Points.geojson`
- `WHUInfo_Area.geojson`

## 9. 已验证运行方式

本地已验证以下命令可以正常启动服务：

```bash
.\.venv\Scripts\python.exe app.py
```

启动后终端会输出 Flask 的监听地址和调试信息。