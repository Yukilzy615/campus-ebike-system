根据你提供的代码和描述，热力图位置错误的核心原因在于**热力图数据格式与百度HeatmapOverlay的要求不完全匹配，以及坐标转换逻辑中存在潜在的二次转换或数据顺序错误**。以下是具体分析和修复方案。

***

### 一、问题根源定位

#### 1. 百度HeatmapOverlay的数据格式要求

百度热力图库要求的 `data` 格式为：

```javascript
[
  {lng: 经度, lat: 纬度, count: 强度值},
  ...
]
```

你在 `renderHeatmap` 中构造 `points` 的代码为：

```javascript
const points = heatData.map(point => ({
    lng: point[1],   // heatData 是 [lat, lng, intensity]
    lat: point[0],
    count: Math.max(0, point[2] || 0)
}));
```

这里假设 `heatData` 的格式是 `[lat, lng, intensity]`。但如果后端返回的数据格式实际上是 `[lng, lat, intensity]`，那么经纬度就会颠倒，导致热力图显示到错误的位置（比如跑到海里或校外）。

#### 2. 坐标转换逻辑混乱

在 `updateHeatmap` 中，对后端返回的数据做了如下处理：

```javascript
if (activeMapProvider === 'baidu' && typeof gcoord !== 'undefined') {
    heatData = heatData.map(point => {
        if (Array.isArray(point) && point.length === 3) {
            const [lat, lng, intensity] = point;
            if (lng < 114.359) {
                // WGS84格式，需要转换为BD09
                const [bdLng, bdLat] = gcoord.transform([lng, lat], gcoord.WGS84, gcoord.BD09);
                return [bdLat, bdLng, intensity];
            } else {
                // 已经是BD09格式，直接使用
                return [lat, lng, intensity];
            }
        }
        return [0, 0, 0];
    });
}
```

这里的问题在于：

- 它假设后端返回的数组顺序是 `[lat, lng, intensity]`，但 `app.py` 中 `get_heatmap_points` 返回的 `points` 确实是 `[lat, lng, intensity]`（见代码 `points.append([lat, lng, int(row['demand'])])`）。所以顺序是对的。
- 但是，**后端返回的数据已经是 BD09 坐标**（因为 `demands.csv` 中的坐标是 BD09 范围：纬度30.528~~30.535，经度114.360~~114.370）。然而转换判断条件 `if (lng < 114.359)` 是基于 WGS84 经度范围（约114.35）来判断的，对于 BD09 坐标（经度>114.36），这个条件为 `false`，所以不会转换，直接返回原值。
- **但这里有一个致命错误**：当判断为 BD09 时，返回的是 `[lat, lng, intensity]`，而后续 `renderHeatmap` 中又将其映射为 `{lng: point[1], lat: point[0]}`。这本身是对的（`point[1]` 是 lng，`point[0]` 是 lat）。所以理论上格式正确。

#### 3. 热力图数据过滤导致点数量不足

`get_heatmap_points` 中使用了 `is_point_in_area` 函数进行过滤，该函数依赖 `point_in_polygon` 射线法算法。如果该算法有误，可能会错误地排除大部分点，导致热力图只显示零星的错误位置点。

#### 4. 百度热力图库未正确加载或调用失败

控制台可能报错 `BMapLib is not defined` 或 `HeatmapOverlay is not a constructor`。你的 `index.html` 中已经引入了热力图库，但需确认脚本在百度地图 API 之后加载，且网络请求成功。

***

### 二、修复方案（按优先级）

#### ✅ 方案一：修正热力图数据格式与坐标转换（推荐）

1. **统一数据格式**：确保传入 `renderHeatmap` 的 `heatData` 始终为 `[lat, lng, intensity]`（与 Leaflet.heat 格式一致），然后在百度热力图分支中正确映射。
2. **简化坐标转换判断**：既然后端数据已经是 BD09，且前端模拟数据在 `USE_MOCK=true` 时也通过 `gcoord` 转换为了 BD09，那么 `updateHeatmap` 中无需再做转换判断。直接信任数据已经是 BD09。

修改 `updateHeatmap` 如下：

```javascript
function updateHeatmap() {
    const timeSlot = document.getElementById('heatmap-time').value || 'morning';
    showProgress('正在生成热力图...');

    if (USE_MOCK) {
        setTimeout(() => {
            let heatData = MOCK_HEATMAP_DATA[timeSlot] || [];
            // 模拟数据是 WGS84，需要转换
            if (activeMapProvider === 'baidu' && typeof gcoord !== 'undefined') {
                heatData = heatData.map(point => {
                    const [bdLng, bdLat] = gcoord.transform([point[1], point[0]], gcoord.WGS84, gcoord.BD09);
                    return [bdLat, bdLng, point[2]];  // [lat, lng, intensity]
                });
            }
            renderHeatmap(heatData);
            hideProgress();
            showToast('热力图更新完成');
        }, 800);
    } else {
        fetch(API_BASE + 'heatmap-data?time=' + timeSlot)
            .then(response => response.json())
            .then(data => {
                let heatData = data.points || [];
                // 后端数据已经是 BD09，格式为 [lat, lng, intensity]，直接使用
                // 过滤掉无效点（可选）
                heatData = heatData.filter(point => 
                    Array.isArray(point) && point.length === 3 &&
                    point[0] >= 30.52 && point[0] <= 30.55 &&
                    point[1] >= 114.35 && point[1] <= 114.38
                );
                renderHeatmap(heatData);
                hideProgress();
                showToast('热力图更新完成');
            })
            .catch(error => {
                console.error('获取热力图数据失败:', error);
                hideProgress();
                showToast('获取热力图数据失败，使用模拟数据');
                // fallback...
            });
    }
}
```

1. **修正** **`renderHeatmap`** **中百度热力图的数据映射**：

```javascript
// 优先使用百度热力图
if (activeMapProvider === 'baidu' && window.BMapLib && window.BMapLib.HeatmapOverlay) {
    try {
        const points = heatData.map(point => ({
            lng: point[1],   // 经度
            lat: point[0],   // 纬度
            count: Math.max(0, point[2] || 0)
        }));
        
        // 过滤掉经纬度为0或无效的点
        const validPoints = points.filter(p => p.lng > 0 && p.lat > 0);
        
        heatLayer = new BMapLib.HeatmapOverlay({
            radius: 20,
            visible: true,
            opacity: 0.8,
            gradient: {
                0.2: 'blue',
                0.4: 'cyan',
                0.6: 'lime',
                0.8: 'yellow',
                1.0: 'red'
            }
        });
        
        map.addOverlay(heatLayer);
        heatLayer.setDataSet({ data: validPoints, max: 100 });
        
        console.log('百度热力图渲染成功，点数:', validPoints.length);
        return;
    } catch (e) {
        console.error('百度热力图渲染失败，降级处理', e);
    }
}
```

#### ✅ 方案二：验证热力图数据正确性（调试用）

在 `renderHeatmap` 开头添加调试代码，用百度地图的 Marker 将热力图数据点绘制出来，检查位置是否正确：

```javascript
// 临时调试：绘制热力图点
heatData.slice(0, 10).forEach(point => {
    const marker = new BMap.Marker(new BMap.Point(point[1], point[0]));
    map.addOverlay(marker);
});
```

如果这些 Marker 位置正确，说明数据没问题，问题出在 HeatmapOverlay 的配置或调用上。

#### ✅ 方案三：检查百度热力图库加载

在控制台输入 `BMapLib.HeatmapOverlay`，确认返回函数。若为 `undefined`，检查 `index.html` 中脚本顺序，确保百度地图 API 在前，热力图库在后。

***

### 三、如果仍无法解决，临时降级为 Leaflet.heat

若时间紧迫，可强制使用 Leaflet.heat 插件渲染热力图，虽然可能与底图有轻微偏移（因坐标系差异），但相对位置通常可接受。修改 `renderHeatmap`，将百度热力图分支注释，直接使用 Leaflet 热力图：

```javascript
// 强制使用 Leaflet 热力图
if (window.L && L.heatLayer) {
    heatLayer = L.heatLayer(heatData, {
        radius: 20,
        blur: 15,
        max: 100,
        gradient: { 0.4: 'blue', 0.6: 'cyan', 0.7: 'lime', 0.8: 'yellow', 1.0: 'red' }
    });
    if (document.getElementById('layer-heatmap').checked) {
        heatLayer.addTo(map);
    }
    return;
}
```

注意：Leaflet 热力图的坐标必须是 `[lat, lng, intensity]`，与当前 `heatData` 格式一致。

***

### 四、总结执行步骤

1. **清理坐标转换逻辑**：移除 `updateHeatmap` 中冗余的条件判断，确保数据格式统一。
2. **修正百度热力图数据映射**：确认 `points` 中 `lng` 和 `lat` 顺序正确。
3. **测试热力图数据点**：用 Marker 临时绘制，验证坐标正确性。
4. **确保库加载**：检查 `BMapLib` 是否存在。
5. **备用降级**：若仍有问题，切换为 Leaflet.heat。

完成以上修改后，热力图应能正确覆盖在校园范围内。若还有偏差，请提供控制台中 `heatData` 的前几个样本数据，以便进一步分析。
